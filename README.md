# quixote-tor-client

TypeScript client for reading [Quixote](https://github.com/bilinearlabs/quixote) indexed data privately. Routes GraphQL queries through Tor — new circuit per query by default, with clearnet fallback when Tor isn't available.
